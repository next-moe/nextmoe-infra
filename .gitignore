dist
.wrangler
.output
.vercel
.netlify
.vinxi
.nuxt
app.config.timestamp_*.js

# Environment
.env
.env*.local
.env*.production
# Runtime secrets — read at runtime via env_file, never committed.
# (*.env.example templates ARE tracked.)
docker/*.env

# dependencies
**/node_modules

# IDEs and editors
/.idea
.project
.classpath
*.launch
.settings/

# Temp
gitignore

# System Files
.DS_Store
Thumbs.db

# turbo
.turbo
.env.goenv

# Root-anchored: a bare name would silently ignore any nested refs/ or tmp/
# source directory anywhere in the tree.
/refs
/tmp

# build
apps/api/build
apps/api/tmp
apps/api/kun-api
apps/api/kun-blog-solid-fiber

# Generated files
apps/account/public/sitemap.xml
apps/account/public/urls.txt

# log
logs

# backup
backup

# prompts
prompts

# reference
/refs

# dumps (contain production user data / password hashes — never commit)
scripts/*.dump
scripts/**/*.dump
scripts/migrated/

.pi/mcp.json
